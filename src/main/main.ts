import initMainGlobals from './mainInit'
import { setupMain } from './mainSetup'

const fileName: string = 'main.ts'
const funcName: string = 'Main Entry'
const area: string = 'main'

initMainGlobals()
setupMain()
