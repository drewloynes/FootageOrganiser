appId: com.drewloynes.footageorganiser
productName: Footage Organiser
directories:
  buildResources: build
win:
  executableName: footage-organiser
  icon: resources/footage-organiser-icon.ico
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  installerIcon: resources/footage-organiser-icon.ico
mac:
  icon: resources/footage-organiser-icon.icns
  category: public.app-category.utilities
dmg:
  artifactName: ${name}-${version}.${ext}
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: github
  owner: drewloynes
  repo: footageorganiser
